// Placeholder web search tool.

export async function webSearchStub(query) {
  return {
    query,
    results: [],
    note:
      "Web search is not enabled in this demo. Answer is based on model knowledge only."
  };
}
